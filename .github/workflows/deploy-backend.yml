name: Deploy Backend

on:
  push:
    branches: [develop, main]
    paths:
      - "backend/**"
      - "shared/**"
      - ".github/workflows/deploy-backend.yml"
  workflow_dispatch: {}

permissions:
  id-token: write # required for AWS OIDC
  contents: read

env:
  AWS_REGION: eu-west-2

jobs:
  deploy:
    runs-on: ubuntu-latest
    # develop -> the "development" GitHub Environment (dev Lambda), main -> "production"
    # (prod Lambda) — each environment holds its own LAMBDA_FUNCTION_NAME variable.
    environment: ${{ github.ref_name == 'main' && 'production' || 'development' }}
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: 24
          cache: npm

      - run: npm ci

      - name: Build backend Lambda bundle
        run: npm run build --workspace backend

      - name: Zip Lambda package
        run: |
          cd backend/dist
          zip -j ../function.zip api.js

      - name: Configure AWS credentials
        uses: aws-actions/configure-aws-credentials@v4
        with:
          role-to-assume: ${{ secrets.AWS_DEPLOY_ROLE_ARN }}
          aws-region: ${{ env.AWS_REGION }}

      - name: Update Lambda function code
        run: |
          aws lambda update-function-code \
            --function-name "${{ vars.LAMBDA_FUNCTION_NAME }}" \
            --zip-file fileb://backend/function.zip

      - name: Wait for update to finish
        run: |
          aws lambda wait function-updated \
            --function-name "${{ vars.LAMBDA_FUNCTION_NAME }}"

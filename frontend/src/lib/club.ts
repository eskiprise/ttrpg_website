/**
 * Where "Написати в Telegram" sends someone. Every join CTA on the site points here,
 * so it's one constant rather than a link repeated per page.
 *
 * TODO: replace with the club's real contact — the public chat invite or an admin's
 * @username. Right now it points at the bot, which works but is a colder landing.
 */
export const CLUB_TELEGRAM_URL = "https://t.me/ttrpgpollbot";

// Lives in the shared package now — the backend needs it too, to match poll titles to
// game systems. Re-exported here so existing imports keep working.
export { formatGameTitle } from "@ttrpg-club/shared";

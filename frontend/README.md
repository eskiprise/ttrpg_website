# Frontend

React + TypeScript + Vite + Tailwind CSS v4. Part of the `ttrpg-club-website` npm
workspace monorepo — see [the root README](../README.md) for setup, environment
variables, architecture, and deployment. There's nothing frontend-specific beyond what's
covered there; this file exists just so the directory isn't empty of docs.
